(function(BB){'use strict';var C=BB.Constants,M=BB.MathUtils;function step(body,dt){var drag=Math.max(0,1-C.AIR_DRAG*dt);body.vx*=drag;body.vy=body.vy*drag+C.GRAVITY*dt;body.x+=body.vx*dt;body.y+=body.vy*dt;body.rotation+=(Math.abs(body.vx)+Math.abs(body.vy)*.18)/body.radius*dt;return body;}function trajectory(start,velocity,time){return{x:start.x+velocity.x*time,y:start.y+velocity.y*time+.5*C.GRAVITY*time*time};}function reflect(velocity,normal,restitution){var dot=velocity.x*normal.x+velocity.y*normal.y;return{x:(velocity.x-2*dot*normal.x)*(restitution||.72),y:(velocity.y-2*dot*normal.y)*(restitution||.72)};}function circleCollision(ball,point,radius){var dx=ball.x-point.x,dy=ball.y-point.y,dist=Math.hypot(dx,dy),min=ball.radius+radius;if(dist>=min||!dist)return false;var nx=dx/dist,ny=dy/dist,v=reflect({x:ball.vx,y:ball.vy},{x:nx,y:ny},.68);ball.x=point.x+nx*min;ball.y=point.y+ny*min;ball.vx=v.x;ball.vy=v.y;return true;}function dragToShot(start,end){var dx=end.x-start.x,dy=end.y-start.y,d=Math.hypot(dx,dy);if(d<18||dy>-8)return{x:0,y:0,power:0};var power=M.clamp(d*8.4,650,1950),verticalRatio=M.clamp(-dy/d,.45,1),horizontalRatio=dx/d;return{x:horizontalRatio*power*.78,y:-verticalRatio*power,power:power};}BB.Physics={step:step,trajectory:trajectory,reflect:reflect,circleCollision:circleCollision,dragToShot:dragToShot};})(window.BB=window.BB||{});
