describe('Save Manager',function(){it('writes and reads progress',function(){BB.SaveManager.save({score:42});assertEqual(BB.SaveManager.load().score,42);BB.SaveManager.clear();});it('returns fallback without save',function(){BB.SaveManager.clear();assertEqual(BB.SaveManager.load('none'),'none');});it('rejects a mismatched version',function(){localStorage.setItem(BB.SaveManager.KEY,JSON.stringify({version:999,data:{score:1}}));assertEqual(BB.SaveManager.load(null),null);BB.SaveManager.clear();});});
