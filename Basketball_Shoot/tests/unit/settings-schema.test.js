describe('Settings Schema',function(){it('accepts values within volume bounds',function(){assertEqual(BB.SettingsSchema.validate({bgmVolume:70}).bgmVolume,70);});it('repairs invalid volume values',function(){assertEqual(BB.SettingsSchema.validate({bgmVolume:101}).bgmVolume,30);});it('repairs invalid themes',function(){assertEqual(BB.SettingsSchema.validate({theme:'broken'}).theme,'cute');});});
