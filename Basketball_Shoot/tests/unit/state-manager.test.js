describe('State Manager',function(){it('allows menu to playing',function(){var s=new BB.StateManager();assertEqual(s.transition('PLAYING'),true);});it('rejects illegal menu to paused transition',function(){var s=new BB.StateManager();assertEqual(s.transition('PAUSED'),false);assertEqual(s.state,'MENU');});it('supports pause and resume',function(){var s=new BB.StateManager();s.transition('PLAYING');s.transition('PAUSED');assertEqual(s.transition('PLAYING'),true);});});
