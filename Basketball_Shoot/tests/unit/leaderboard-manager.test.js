describe('Leaderboard',function(){it('sorts scores from high to low',function(){var rows=BB.Leaderboard.normalize([{name:'a',score:2},{name:'b',score:9}]);assertEqual(rows[0].score,9);});it('keeps only the top ten',function(){var rows=[];for(var i=0;i<14;i++)rows.push({name:'p',score:i});assertEqual(BB.Leaderboard.normalize(rows).length,10);});it('escapes unsafe nickname markup',function(){assertEqual(BB.Leaderboard.escapeName('<img>'),'&lt;img&gt;');});});
