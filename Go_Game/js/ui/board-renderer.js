window.GoGame=window.GoGame||{};GoGame.BoardRenderer=(()=>{let canvas,ctx,hover=null,metrics=null;const letters="ABCDEFGHJKLMNOPQRST";function css(v){return getComputedStyle(document.documentElement).getPropertyValue(v).trim()}function init(){canvas=document.getElementById("go-board");if(!canvas)return;ctx=canvas.getContext("2d");canvas.onpointermove=e=>{hover=point(e);render()};canvas.onpointerleave=()=>{hover=null;render()};canvas.onclick=e=>{const p=point(e);if(p)GoGame.GameUI.humanMove(p.x,p.y)}}function calc(size){const rect=canvas.getBoundingClientRect(),dpr=window.devicePixelRatio||1;canvas.width=Math.floor(rect.width*dpr);canvas.height=Math.floor(rect.height*dpr);ctx.setTransform(dpr,0,0,dpr,0,0);const pad=rect.width/(size+1),cell=(rect.width-pad*2)/(size-1);metrics={w:rect.width,pad,cell};return metrics}function point(e){const game=GoGame.State.get().game;if(!game||!canvas)return null;const m=metrics||calc(game.size),r=canvas.getBoundingClientRect();const x=Math.round((e.clientX-r.left-m.pad)/m.cell),y=Math.round((e.clientY-r.top-m.pad)/m.cell);if(x<0||y<0||x>=game.size||y>=game.size)return null;const px=m.pad+x*m.cell,py=m.pad+y*m.cell;if(Math.hypot(e.clientX-r.left-px,e.clientY-r.top-py)>m.cell*.46)return null;return{x,y}}function stars(size){if(size===9)return[[2,2],[6,2],[4,4],[2,6],[6,6]];if(size===13)return[[3,3],[9,3],[6,6],[3,9],[9,9]];return[[3,3],[9,3],[15,3],[3,9],[9,9],[15,9],[3,15],[9,15],[15,15]]}function drawStone(x,y,color,r,alpha=1){const m=metrics,px=m.pad+x*m.cell,py=m.pad+y*m.cell;ctx.save();ctx.globalAlpha=alpha;const grad=ctx.createRadialGradient(px-r*.35,py-r*.45,r*.1,px,py,r);if(color===1){grad.addColorStop(0,"#555");grad.addColorStop(.45,css("--stone-black"));grad.addColorStop(1,"#000")}else{grad.addColorStop(0,"#fff");grad.addColorStop(.65,css("--stone-white"));grad.addColorStop(1,"#c8c8c8")}ctx.fillStyle=grad;ctx.beginPath();ctx.arc(px,py,r,0,Math.PI*2);ctx.fill();ctx.strokeStyle=color===1?"rgba(255,255,255,.18)":"rgba(0,0,0,.35)";ctx.lineWidth=1;ctx.stroke();ctx.restore()}function render(){const game=GoGame.State.get().game;if(!game||!canvas)return;const size=game.size,m=calc(size),r=m.cell*.43;ctx.clearRect(0,0,m.w,m.w);ctx.fillStyle=css("--bg-board");ctx.fillRect(0,0,m.w,m.w);ctx.strokeStyle=css("--line-color");ctx.lineWidth=1.2;for(let i=0;i<size;i++){const p=m.pad+i*m.cell;ctx.beginPath();ctx.moveTo(m.pad,p);ctx.lineTo(m.w-m.pad,p);ctx.moveTo(p,m.pad);ctx.lineTo(p,m.w-m.pad);ctx.stroke()}ctx.fillStyle=css("--line-color");for(const [x,y] of stars(size)){ctx.beginPath();ctx.arc(m.pad+x*m.cell,m.pad+y*m.cell,Math.max(3,m.cell*.08),0,Math.PI*2);ctx.fill()}if(game.settings.showCoords){ctx.fillStyle=css("--text-secondary");ctx.font="12px sans-serif";ctx.textAlign="center";ctx.textBaseline="middle";for(let i=0;i<size;i++){const p=m.pad+i*m.cell;ctx.fillText(letters[i],p,10);ctx.fillText(String(size-i),10,p)}}for(let i=0;i<game.board.length;i++){const c=game.board[i];if(c){const {x,y}=GoGame.Board.xy(i,size);drawStone(x,y,c,r)}}if(hover&&!game.board[GoGame.Board.idx(hover.x,hover.y,size)]&&game.currentTurn===game.settings.playerColor&&!game.aiThinking)drawStone(hover.x,hover.y,game.currentTurn,r,.38);if(game.settings.showLastMove&&game.lastMove){ctx.strokeStyle=game.lastMove.color===1?"#fff":"#111";ctx.lineWidth=2;ctx.beginPath();ctx.arc(m.pad+game.lastMove.x*m.cell,m.pad+game.lastMove.y*m.cell,r*.32,0,Math.PI*2);ctx.stroke()}}window.addEventListener("resize",GoGame.Utils.debounce(render,120));return{init,render}})();
