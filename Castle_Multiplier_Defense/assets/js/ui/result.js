(function (root) {
    "use strict";
    var cg = root.CastleGame = root.CastleGame || {};
    var Result = cg.Result = {};
    var outcome = "win"; var snapshot = null;
    Result.show = function (result, stats) {
        outcome = result; snapshot = stats || {}; var isFinalWin = result === "win" && Number(snapshot.level) >= cg.Level.count; var screen = document.getElementById("result-screen"); screen.dataset.result = result; var icon = document.getElementById("result-icon"); var title = document.getElementById("result-title"); var copy = document.getElementById("result-copy"); var eyebrow = document.getElementById("result-eyebrow"); var primary = document.getElementById("result-primary-button"); var primaryLabel = primary.querySelector("span:last-child"); var titleKey = result === "win" ? "result.winTitle" : "result.loseTitle"; var copyKey = result === "win" ? "result.winCopy" : "result.loseCopy"; var primaryKey = result === "win" ? (isFinalWin ? "result.menu" : "result.next") : "result.retry"; icon.textContent = result === "win" ? "✦" : "☁"; eyebrow.textContent = cg.I18n.t("result.eyebrow"); title.setAttribute("data-i18n", titleKey); copy.setAttribute("data-i18n", copyKey); primaryLabel.setAttribute("data-i18n", primaryKey); title.textContent = cg.I18n.t(titleKey); copy.textContent = cg.I18n.t(copyKey); primary.dataset.action = result === "win" && !isFinalWin ? "next-level" : result === "win" ? "result-menu" : "retry"; primaryLabel.textContent = cg.I18n.t(primaryKey); primary.hidden = false; var secondary = document.querySelector(".result-actions .button:nth-child(2)"); secondary.hidden = result !== "win"; var statsNode = document.getElementById("result-stats"); statsNode.textContent = ""; [["result.time", cg.Utils.formatTime(stats.time || 0)], ["result.maxMultiplier", "x" + cg.Utils.formatNumber(stats.maxMultiplier || 1)], ["result.damage", cg.Utils.formatNumber(stats.damage || 0)], ["result.shots", cg.Utils.formatNumber(stats.shots || 0)], ["result.hitRate", cg.Utils.formatPercent(stats.hitRate || 0)], ["result.combo", cg.Utils.formatNumber(stats.combo || 0)]].forEach(function (entry) { var item = document.createElement("div"); item.className = "result-stat"; var label = document.createElement("small"); label.textContent = cg.I18n.t(entry[0]); var value = document.createElement("strong"); value.textContent = entry[1]; item.appendChild(label); item.appendChild(value); statsNode.appendChild(item); });
    };
    Result.next = function () { var next = Math.min(cg.Level.count, (snapshot && snapshot.level || 1) + 1); cg.App.beginBattle(next, false); };
    Result.retry = function () { cg.App.beginBattle(snapshot && snapshot.level || 1, false); };
    Result.getOutcome = function () { return outcome; };
}(window));
